# Sample Markdown Document

This document checks common Markdown rendering features.

## Checklist

- Render headings clearly
- Preserve paragraph spacing
- Display inline code like `renderPreview(file)`
- Render fenced code blocks

```js
const title = "English Markdown Preview";
console.log(title);
```

| Format | Purpose |
| --- | --- |
| Markdown | Rich text preview |
| CSV | Table preview |
| JSON | Structured data preview |
