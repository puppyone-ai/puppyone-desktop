# Platform

Platform modules provide editor-scoped capability brokers, security policy,
execution/edit sessions, indexing workers, and CodeMirror host adapters. They
may depend on core leaf contracts where required, but never on composition or
a concrete Markdown feature/widget.
