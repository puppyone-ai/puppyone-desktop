# Platform

Platform modules provide editor-scoped capability brokers, security policy,
execution/edit sessions, and CodeMirror host adapters. They may depend on core
contracts by type, but never on a concrete Markdown feature or widget.
