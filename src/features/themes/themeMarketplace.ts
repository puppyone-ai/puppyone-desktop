/**
 * Product-owned theme gallery entry point. Keep this nullable until the public
 * gallery is ready; configuring one HTTPS URL enables the existing Add Theme
 * control without changing the settings UI.
 */
export const THEME_MARKETPLACE_URL: string | null = null;
